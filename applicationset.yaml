apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: all-applications
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - matrix:
      generators:
      - list:
          elements:
          - env: devl
            cluster: https://kubernetes.default.svc
            targetRevision: sprint/sprint-25
          - env: test
            cluster: https://kubernetes.default.svc
            targetRevision: test
          - env: prod
            cluster: https://kubernetes.default.svc
            targetRevision: prod
      - git:
          repoURL: https://dev.azure.com/YOUR-ORG/YOUR-PROJECT/_git/argocd-bootstrap
          revision: main
          files:
          - path: "apps-config.yaml"
  template:
    metadata:
      name: '{{.name}}-{{.env}}'
      labels:
        app: '{{.name}}'
        environment: '{{.env}}'
    spec:
      project: default
      source:
        repoURL: '{{.repo}}'
        targetRevision: '{{.targetRevision}}'
        path: '{{.manifests_path}}/{{.env}}'  # This will become: src/kubernetes/overlay/devl
      destination:
        server: '{{.cluster}}'
        namespace: '{{.namespace}}-{{.env}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
